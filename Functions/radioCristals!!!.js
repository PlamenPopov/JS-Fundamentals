function solve(input){

let final=+input[0];

let cut=(x)=>x/4;
let lap=(x)=>x*0.8;
let crind=(x)=>x-20;
let etch=(x)=>x-2;
let xray=(x)=>x+1;
let transport=(x)=>parseFloat.Int(x);

input=input.map(Number);

for (let i = 1; i < input.length; i++) {
      detail=input[i]; 
      console.log(`Processing chunck ${detail} microns`);
      
}

}

solve(['1375','50000']);